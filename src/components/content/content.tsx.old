import React from 'react';
import { Grid, Row, Col } from 'react-flexbox-grid';
import Clip from './clip'
import ITwitchClip from '../../models/ITwitchClip';

class Content2 extends React.Component {
    state = {
        clips: []
    }

    componentDidMount() {
        
        // fetch("https://api.twitch.tv/kraken/clips/top?channel=shroud&period=month&trending=false&limit=5",{
        // headers: new Headers({
        //     'Accept': 'application/vnd.twitchtv.v5+json',
        //     'Client-ID': 'pglc4rfh9r74bnlezx2ftnbswss3bp'
        //     })
        // })
        // .then(response => {
        //     if (!response.ok){
        //         throw Error(response.statusText)
        //     }
        //     return response.json()
        // })
        // .then(
        //     data => {
        //         this.setState({ clips: data.clips})
        //     })
        // .catch(error => console.error(error))
    }

    render() {
        return (
          <div style={{margin: 20}}>
          <Grid fluid>
              <Row>
                  {this.state.clips.map((clip, index) => {
                      return (
                      <Col key={index}>
                        <Clip clip={clip as ITwitchClip}/>
                      </Col>)
                  })}
              </Row>
            </Grid>
          </div>
        );
      }
}

export default Content2;